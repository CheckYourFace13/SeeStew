/** Bing IndexNow API key — must match public/<key>.txt and Bing Webmaster. */
export const INDEXNOW_KEY = "39f4a09627794f9d938e051350d8169e";

/** Exact key string only (no trailing newline) — matches Bing’s sample key file. */
export const indexNowKeyFileBody = INDEXNOW_KEY;
